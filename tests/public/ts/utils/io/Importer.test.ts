import "jest";

import {Importer} from "../../../../../site/public/ts/utils/io/Importer";

import {CircuitDesigner} from "../../../../../site/public/ts/models/CircuitDesigner";

describe("Importer", () => {
    // async function LoadFileContents(path: string): Promise<string> {
    //     return new Promise<string>((resolve, reject) => {
    //
    //     });
    // }

    // TODO: this
    // test("Basic (v1.1) Circuit Load", (done) => {
    //     const designer = new CircuitDesigner(0);
    //
    //     Importer.LoadCircuit(designer, "");
    //
    //     expect(designer.getObjects().length).toBe(2);
    //     expect(designer.getWires().length).toBe(1);
    // });
});
